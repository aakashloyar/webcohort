// interface Config {
//     readonly endpoint: string;
//     readonly apiKey: string;
//   }
  
//   const config: Readonly<Config> = {
//     endpoint: 'https://api.example.com',
//     apiKey: 'abcdef123456',
//   };

//   //config.apikey=''; //cannot do this bcz it is readonly
//   //const not let you assign new object but parts can be reassigned here comes readonly to avoid this